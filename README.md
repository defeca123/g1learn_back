### Rode os seguintes comandos para instalar as dependencias:

    composer install
    npm install
    composer require laravel/jetstream
    php artisan jetstream:install livewire

### Para rodar o projeto:

    php artisan serve
    
